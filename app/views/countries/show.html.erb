<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render object: @country, partial: "card", as: :country %>
  </div>
</div>

  <!-- Show Page Customization starts -->
<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "countries/country_itineraries" %>
  </div>
</div>

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "countries/country_cities" %>
  </div>
</div>
  <!-- Show Page Customization end -->
